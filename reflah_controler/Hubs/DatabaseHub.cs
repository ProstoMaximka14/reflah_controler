using Microsoft.AspNetCore.SignalR;

namespace reflah_controler.Hubs
{
    public class DatabaseHub : Hub
    {
        // Пустой класс - он нужен только как "точка подключения"
        // SignalR сам добавит всю необходимую функциональность
    }
}